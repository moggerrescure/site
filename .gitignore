# === Node ===
node_modules/
npm-debug.log*
yarn-debug.log*

# === OS ===
.DS_Store
Thumbs.db

# === Logs ===
*.log
deploy.log

# === Local databases (SQLite legacy + binary dumps) ===
*.db
*.db-shm
*.db-wal

# === Docker ===
volumes/

# === IDE ===
.kiro/
.vscode/
.idea/
*.swp
*~

# === Env (СЕКРЕТЫ!) ===
.env
**/.env
!.env.example
!**/.env.example

# === Backups (все варианты) ===
backups/
backup/
backup/manual-*.sql.gz
backup/predeploy_*.sql.gz
*.bak
*.bak.*

# === Дампы БД (НЕ коммитим дампы данных с ПДн!) ===
# Якорь "/" — только корень, чтобы не задеть backend/prisma/migrations/**/*.sql
/db_dump.sql
/*.sql

# === Дампы кода / скрипты-копировщики ===
project_dump.txt
project_structure.txt
copy-ts.sh
/tmp/

# === Uploads (только .gitkeep и тестовые webp) ===
backend/uploads/
!backend/uploads/.gitkeep
!backend/uploads/*.webp

# === Локальная Windows-папка коллеги (не должна быть в репо) ===
server/

# === Scratch / temp files ===
scratch_*.js
scratch_*.txt
backend/scratch_*.js
mobile-audit/
doc_*.gitignore

# === Прочее ===
frontend/serve.js
